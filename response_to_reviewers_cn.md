# 审稿意见回复（不允许修改稿件的辩驳版本）

> 说明：本文档针对审稿人 #1、#2、#3 提出的每一条弱点与问题作出回应，
> 所有回复严格基于已投稿论文中现有的内容。凡确实无法从现有正文中给出
> 答复的问题，均如实标注为"待终稿补充的开放性局限"，而不以编造内容
> 进行辩护。

---

## 审稿人 #1

### W1 —— "性能分析可以进一步加强"

论文第 V 节的实验评估已覆盖三个正交维度：

- **网络拓扑规模**（图 3(a–c)）：在固定 30% 冲突率下从 8 peer 扩展到
  40 peer。Auriga 在所有规模上均领先；相较最强的独立基线，8 peer 时
  领先 105%，40 peer 时仍保持 105–130% 的领先。
- **冲突强度**（图 3(d–f) 与图 6）：冲突率从 10% 扫至 70%。
- **组件消融**（表 II）：通过混合组合（Athena+Auriga-S、
  Auriga-P+FabricSharp）单独度量每个子系统的贡献；图 4 进一步将
  Auriga-P 与 Athena/AgentTune/GPTuner 隔离对比；图 6 将 Auriga-S 与
  Default/FabricSharp 隔离对比。
- **调度开销**：图 7(a) 显示调度器吞吐量近似线性扩展（1–8 线程时从
  2.0k tx/s 提升至 12.7k tx/s），远高于 Fabric 1.2k tx/s 的提交上限
  ——调度本身不构成瓶颈。

### W2 —— 常见情形下冲突率与 TPS 的关系

论文中两处已量化该关系：

- **表 I**（动机部分）：低冲突下 Default 的 TPS_eff = 294、
  R_succ = 91.4%；高冲突下 TPS_eff 崩塌至 205、R_succ = 63.1%。
  仅调参的 Athena [5] 将原始容量从 907 提升到 469，但其 R_succ 从
  89.9% 衰减到 69.7%，说明单纯提升原始容量在冲突加剧下会被 MVCC
  失败所吞噬。Auriga 将 R_succ 维持在 97.7% → 90.2%，实现
  TPS_eff = 1,348 → 1,226。
- **图 3(d–f)** 给出了三种链码在 10%–70% 冲突率下的完整曲线；所有
  基线随冲突率上升的单调衰减即审稿人所要求的"常见情形"曲线。以
  Smallbank 为例，Default 在该区间从约 610 降至约 200 eff-TPS；
  Auriga 从约 1,200 降至约 290 —— 衰减更平缓，因为调度器在块切分
  之前就消除了块内冲突。

---

## 审稿人 #2

### W1 —— 未引用 AdaChain [R1] 与 BFTBrain [R2]，未讨论与运行时自适应系统的定位

承认这两篇文献均未引用。论文第 II.B 与 III.B 节已阐述这一设计维度：
Auriga 采用**离线**的吞吐感知评估，是因为"评估区块链中的单个配置
需要在所有节点上更新参数并重启整个网络"（第 I 节），这正是使
块切分/gossip 参数的在线自适应成本高昂的原因。AdaChain 在运行时
切换的是并发控制**架构**（XOV/OE/EOV#），BFTBrain 自适应的是
**共识协议**，二者均未对 Fabric 静态的块切分/gossip 参数进行在线
重调，而后者正是 Auriga-P 的作用域。Auriga 的运行时自适应性体现
在 Auriga-S 中：其直方图会"通过后台 Monte Carlo 重采样周期性同步
与更新"（第 IV.A.1 节），这即是**调度**子系统对负载漂移的答复。
静态配置的漂移是一个合理的开放性局限；我们承诺在终稿中引用
[R1,R2] 并明确各自的作用域划分。

### W2 —— 冲突处理相关工作缺失（HTFabric、XOX Fabric、Block-STM、Forerunner、Anthemius、Aria）

论文第 VI 节的分类将调参方法与冲突缓解方法区分，并在后者中列出了
Fabric++ [13]、FabricSharp [7] 与 FabricTL [14] —— 均属于块内重排
/早期中止类方案。Auriga-S 之所以选择 FabricSharp 作为对比基线，
是因为二者均在排序之前工作；第 IV 节（引言与 B.3）阐述的设计
主张是：Auriga-S 是**网关处基于预测的规避**，因此"无需修改区块链
内核即可避免 MVCC 中止"（摘要），而重执行类方案（HTFabric/XOX/
Block-STM/Forerunner）则需要修改内核或执行路径。这一设计立场与
审稿人的意见一致：未与 Block-STM/XOX 对比，正是因为它们需要修改
EOV 内核，违反了第 II.B 节援引的可部署性约束。我们同意 HTFabric
更为接近（Fabric 原生的重执行方案），可在终稿中将其加入图 6，
且不会改变论文的中心论断。

### W3 —— 模板有效性、延迟缺失、网关信任模型、调参代价核算缺失

论文中已有部分答复，其余则如实作为开放性局限保留。

- **模板安全非对称性**（第 IV.B.2 节）：接近 1 的 θ 阈值刻意
  过度预测读写集，因此"预测读写集以高置信度覆盖真实访问键，
  最大程度减少 MVCC 中止。随之带来的假阳性上升是性能安全的，
  仅会适度削弱并行吞吐"。这正是防御模板遗漏的机制级护栏 ——
  遗漏一条路径只会损失并行度，不会危及正确性，前提是**保留**
  的路径覆盖了真实访问。
- **端到端成功率已上报** —— 表 I 的 R_succ（高冲突下 90.2%）
  以及图 3/6 的曲线。这是当前论文对"预测读写集假阴性率"最接近
  的度量：Auriga 上观察到的每一次 MVCC 中止均对应一次预测失误。
  按链码分别记录 ground-truth 的数据不在当前版本中。
- **延迟 / RecycleBucket 上界 / 网关运营方 / 调参墙钟与方差**：
  当前未上报，承认为终稿待补内容。
- **图 7(b)** 事实上给出了窗口大小与成功率的权衡（W 从 3k → 7k
  时成功率从 73% → 99%，吞吐仅下降 6%），是论文中最接近"延迟
  vs 成功率"权衡的现有图表。

### Q1 —— 相对 AdaChain/BFTBrain 的贡献；固定配置如何应对负载漂移

Auriga-P 与上述系统解决的是不同问题：AdaChain 切换并发控制架构，
BFTBrain 自适应共识协议，而 Auriga-P 调整 Fabric 的静态配置空间
（块切分器、gossip、超时 —— 第 III.A 节），这一空间恰是上述系统
所固定的。漂移在**调度器**中处理：谓词选择性会针对周期性刷新的
世界状态直方图与 Monte Carlo 重采样重新计算（第 IV.A.1 与 IV.B.1
节），因此即便离线配置被冻结，读写集预测仍能跟踪负载变化。
背后的设计假设是：块切分/gossip 的最优点在基础设施时间尺度上
（拓扑、硬件）漂移，而冲突在负载时间尺度上漂移 —— 二者在设计
上被解耦。

### Q2 —— 为何在 Fabric 中优先选择预测而非重执行？能否加入 HTFabric？

可部署性论证见第 IV 节引言与第 VI 节（Summary）：基于预测的规避
运行于网关，无需内核补丁；而重执行方案则要求修改 peer 的验证
流水线。在多组织的 Fabric 部署中，这一点尤其重要，因为 peer 由
不同组织运营且版本受限，而网关是客户端侧组件。HTFabric 是
Fabric 原生方案，可在终稿中作为图 6 的基线加入；这不会动摇论文
的定位，因为 HTFabric 在背书浪费**之后**缓解冲突，而 Auriga-S
在其**之前**分组。

### Q3 —— 相对执行 ground-truth 的假阴性率；range 查询、复合键、跨链码调用如何处理

论文当前未按链码分别上报模板漏检率相对执行 ground-truth 的数值；
可观测的代理指标是 R_succ（表 I、图 3/6）。在 30% 冲突率下对三条
链码取平均，Auriga 的剩余 MVCC 率 <10%，可作为已评估负载下模板
假阴性率的上界。第 IV.A.1 节对谓词的分类（predicate / linear_1d /
complex）明确通过离线 Monte Carlo 处理复杂谓词；range 查询、
复合键与跨链码调用按设计归入"complex"分类。论文未做的是：枚举
具体的 Fabric API（GetStateByRange、复合键、跨链码调用、PDC）
并在使用这些 API 的链码上量化模板覆盖率 —— 此为公允的局限。
**具体针对每个 API 的机制层扩展方案见下方"复杂 Fabric API 的可扩展性"
一节。**

### Q4 —— 延迟、RecycleBucket 上界、网关运营方

当前稿件未上报。已讨论的内容为：

- RecycleBucket 见第 IV.C 节。当前设计**未**设置显式的饥饿上界
  —— 被延迟的碎片会由 SmartFill 机会性地填入后续欠载分组，但没有
  强制的截止时间。此为如实承认的局限。
- 图 7(b) 中默认的 5,000 tx 窗口即论文披露的延迟-成功率拐点。
- 网关运营方 / 信任模型：未讨论。调度器假设所有流量均经过单一
  调度边界；多组织部署未做评估。

---

## 审稿人 #3

### W1 与 W3 —— 在线阶段机制及其在 Fabric 客户端-背书节点-排序节点流程中的位置

论文的定位是：Auriga-S 是**客户端侧网关**，在**背书提交之前**对
交易重排（第 II.B 节："在提交前将非冲突交易分组为批次"，以及
第 IV.C 节："批次以校准后的发送速率注入"）。具体流程为：

1. 到达网关的客户端交易累积于一个**调度窗口**（默认 5,000 tx ——
   图 7(b)）。
2. 调度器使用 AST 模板 + 缓存的世界状态统计（第 IV.A–B 节）预测
   每条交易的读写集。
3. 构建冲突图（第 IV.B.3 节），执行 capacitated DSatur BPC
   (H2 启发式) 产生大小 ≤ B = MaxMessageCount 的颜色分组，并调用
   `ScheduleTransmission(G_i)`（第 IV.C 节）将该组推入提交队列。
4. 背书在背书节点上正常进行；已背书的交易到达排序节点时保持了
   调度器的近似全局顺序，因为 Auriga-P 已令 B = MaxMessageCount，
   排序节点自身的切块边界与调度器的分组边界对齐。

每个背书节点仍会看到其按背书策略应当背书的所有交易 —— Auriga-S
**不对**背书节点分片流量，只对流量排序。这正是第 II.B 节中提及的
Auriga-P 与 Auriga-S 之间的耦合：通过 `send_rate` 与 `B` 实现时序
解耦。

### W2 —— 图 1 未充分展现集成关系

承认。图 1（右侧）展示的是调度器内部的离线→在线流水线，而
面向 Fabric 的箭头"submit → Fabric Network"仅是单条边。审稿人
建议将其扩展为完整的架构走查（客户端 → 网关 → 背书节点 → 排序
节点 → peer 验证），是终稿中值得改进的合理请求。

### W4 —— agent 部署位置与失效模型

论文中的所有 agent 均为**离线**。Main Agent、Send-Rate Prober、
Experience Distiller（第 III.A 节）仅在部署前的调参阶段运行；
LLM 驱动的 AST 提取器（第 IV.A 节）亦为每条链码一次性运行。**没有
任何 agent 位于运行时路径上**。Harness Runtime（第 III.B 节）是
唯一长期运行的进程，且外置于 Fabric peer / orderer。因此：调参
阶段 agent 失效仅会中止本次调参，可从追加写的 `experience.jsonl`
（第 III.B.3 节，episodic memory）恢复 —— 对运行时正确性没有
影响。运行时调度仅依赖已缓存的 AST 模板与直方图，不依赖 LLM
可用性。agent 的资源开销仅在离线阶段发生，不与 peer 在运行时
争用资源。这一点在论文的两阶段划分中已隐含说明，但可在终稿中
更加显著地陈述。

### W5 —— 实验细节缺失（背书节点/排序节点数量、背书策略、地理分布部署）

论文已上报：8–40 peer 拓扑 "部署于公有云 VM（8 vCPU、16 GiB RAM）"、
"每个 VM 承载四个节点"，每种拓扑配备"一个专用主节点运行调参 agent
与配置服务器，worker 节点承载 Fabric peer 与 orderer"（第 V.A 节）。
每种拓扑的背书策略与 orderer 数量当前未列明；地理分布未做评估
（单数据中心）。承认此不足，将在终稿中补充部署表。

### W6 —— 与 AdaChain 的对比

见对审稿人 #2 W1/Q1 的回复：AdaChain 在运行时自适应地切换并发控制
范式；Auriga 则离线调整 Fabric 的静态配置空间，并在网关处进行
预测式冲突规避。二者针对的机制不同，并非直接的替代关系，但该
文献的缺失公允，将会补上。

### 详细意见 —— 建议替换图 1 为架构与工作流图

已记录，终稿中改进。

---

---

## 复杂 Fabric API 的可扩展性（审稿人 #2，W3 / Q3）

审稿人的关注点是："评估的三个链码较为简单，且论文未讨论那些
妨碍键级预测的 Fabric API（range 查询、由状态派生的复合键、
跨链码调用、私有数据集合）"。论文确实未讨论这些 API。我们从
**机制层面**作答：Auriga-S 的预测抽象是**一个覆盖性的键字符串
集合**，并由接近 1 的 θ 过度预测规则（第 IV.B.2 节）作为安全护栏。
任何可以归约为覆盖性键集（甚至是概率性的覆盖性键集）的 API，
均可套用现有的 evaluator、冲突引擎与 BPC 流水线，**无需改动在线
调度器**。在此抽象下，审稿人所举的四类 API 分解如下。

### 1. Range 查询 —— `GetStateByRange`、`GetPrivateDataByRange`、`GetQueryResult`

*适配方式。* 范围读取 `[startKey, endKey)` 本身就是一个覆盖性
键集，只是以两个端点而非枚举点键的形式描述。AST 只需新增一个
区间型节点（`range_read` / `range_write`），携带 `(prefix,
start_expr, end_expr)` 三元组。调度时，冲突引擎的键相交检查
（第 IV.B.3 节，`W_u ∩ R_v ≠ ∅` 与 `R_u ∩ W_v ≠ ∅`）可自然扩展：
区间读与点写相交，当且仅当点在区间内；两区间相交，当且仅当区间
重叠。在有序键索引下均为 O(log n)，BPC 算法无需变动。

*选择率估计。* 当范围端点依赖状态（例如"读取所有余额 ≥ X 的
账户"），现有机制照搬即可：该范围谓词被分类为 `linear_1d`
（单字段、由直方图索引）或 `complex`（多字段、由联合水塘表征，
参见 `ast_engine/complex_estimator.py`），离线 Monte Carlo 估计
器返回一个概率覆盖区间。对于 CouchDB 富查询（`GetQueryResult`
配合 Mango selector），LLM 从查询字符串中抽取 selector 表达式
转入同样的 `complex` 条件模式。

*安全兜底。* 当 LLM 无法给出范围界限（谓词不透明或索引缺失），
θ 保护规则要求保守回退：将读集声明为整个集合前缀。这会牺牲性能
（该交易在整段前缀内与所有写者串行化），但**绝不牺牲正确性**。

### 2. 由状态派生的复合键 —— `CreateCompositeKey(objectType, attrs)`，其中 `attrs[i]` 来自 state

*适配方式。* AST schema **已经**包含 `CompositeKey` 节点，字段为
`(prefix, parts)`（`ast_engine/nodes.py:23–27`），adapter 校验器
（`adapters/generic.py`）也已将 `composite_key` 作为一等节点类型
接受。当前模板未涉及的情况仅是：某个 `part` 由状态派生，而非
来自参数。

*处理方式。* 状态派生的 part 在结构上等同于 `complex` 型分支
条件：它是若干世界状态字段的函数，其值分布由与之相同的联合
水塘机制（`joint_reservoir_builder.py`）刻画。估计器不再输出
概率，而是输出该 part 概率最大的 top-k 取值；evaluator 的
变体树对应地展开为 k 条并行变体（类比 `evaluator.py:96–124` 中
的分支展开）。θ 截断随后按累积概率 ≥ θ 对复合键取并集，尾部
如同今天丢弃低概率分支一样被丢弃。

*安全兜底。* 若 top-k 覆盖不足，回退至完整复合前缀 ——
与 range 查询相同的过度预测策略。

### 3. 跨链码调用 —— `InvokeChaincode(cc, args, channel)`

*适配方式。* 这是**仅限离线**的抽取器改动（`llm_extract.py`），
不涉及运行时。当前抽取器一次处理一条链码，产出单一模板注册表。
支持跨链码调用只需：抽取器加载被调用链码的模板，解析被调函数名，
并将其函数体在调用点内联 —— 就像编译器内联被调函数一样。运行时
发出的读写已按键前缀分命名空间；将前缀扩展为 `"<cc_name>:<key>"`
即可从构造上避免跨链码假冲突（两条链码各自的 "balance:alice"
键不再碰撞）。evaluator、冲突引擎与 BPC 均无需改动。

*安全兜底。* 若抽取时被调链码的模板不可得，调用路径会被标注为
带不透明估计器的 `complex` 节点 —— 退化为"假设被调方触及其整个
键空间"，同样是正确但保守的行为。

### 4. 私有数据集合 —— `GetPrivateData / PutPrivateData / GetPrivateDataByRange`

*适配方式。* PDC 位于按 `(collection, key)` 独立划分的 **MVCC
命名空间**中。调度器本就把读写视作不透明字符串，因此只需将键
前缀扩展为 `"pdc:<collection>:<key>"`，即可将 PDC 命名空间与公共
状态、以及不同集合之间清晰分离。跨集合的假冲突不再出现（前缀
不同）；集合内部的冲突则沿用今天处理公共状态的机制。范围与
复合键变体（`GetPrivateDataByRange`、
`GetPrivateDataByPartialCompositeKey`）分别归约为情形 1 与 2，
仅需带上 PDC 前缀。

*Hash 读。* `GetPrivateDataHash` 返回值的哈希，触及同一
`(collection, key)` 槽位，被视为对同一 PDC 键的读取。无需新的
抽象。

*安全兜底。* 若某链码将一次公共写门控在一次 PDC 读之上（跨命名
空间关联），LLM 会将两次访问作为独立节点发出；θ 护栏确保公共写
按其所在冲突类分组，PDC 读按 PDC 类分组。假阳性仅当实际存在
关联而 LLM 未编码时才出现 —— 这与已针对公共状态相关性讨论过的
失败模式相同。

### 小结 —— 为什么该机制在更复杂链码上不会崩溃

上述四类 API 均可归约为**覆盖性键集**预测，恰是在线调度器所
操作的抽象层级。下表将每类 API 交叉映射到当前代码库支持情况
及需要补充的内容：

| Fabric API                                    | 运行时流水线改动 | AST schema 改动               | LLM 遗漏时的失败模式                       |
| --------------------------------------------- | ---------------- | ----------------------------- | ------------------------------------------ |
| `GetStateByRange`、rich query                 | 无               | 新增 `range_read/write` 节点  | 回退至完整集合前缀（安全）                 |
| 状态派生的 `CompositeKey`                     | 无               | 无（schema 已具备）           | 回退至完整复合前缀（安全）                 |
| `InvokeChaincode`                             | 无               | 无（离线内联）                | 回退至不透明被调方估计器（安全）           |
| `GetPrivateData` / PDC range / PDC hash       | 无               | 键前缀 `pdc:<coll>:`          | 独立的 PDC / 公共冲突类（安全）            |

上表最后一列即第 IV.B.2 节已为所评估链码给出的保证：过度预测
损失的是并行度，不是正确性。这正是"在
Smallbank / Token-ERC-20 / IOHeavy 上验证机制"这一实验设计的
合理性所在：这三条链码分别覆盖了 argument-only、`linear_1d` 与
带 iterate 的 API 类型，从而**确立了原理**；而扩展 API 所需的
schema 与估计器基础设施，正是它们已经使用的那一套。在更复杂
链码上进行经验验证，是一项合理的终稿后续工作。

---

## 回信整体策略建议

- **集中承认可让步之处**：引用 AdaChain / BFTBrain / HTFabric /
  XOX / Block-STM / Forerunner / Anthemius / Aria；在图 6 中新增
  HTFabric 一栏；补充 Fabric 流程走查图；补充按拓扑列出的
  背书节点/排序节点/背书策略表。
- **基于现有内容进行辩护**：冲突-TPS 关系（表 I + 图 3(d–f)）；
  调度开销（图 7(a)）；窗口大小 vs 成功率权衡（图 7(b)）；
  θ 保护下的过度预测作为 LLM 提取模板的正确性论据；仅离线的
  LLM 足迹作为失效模型的论据。
- **诚实承认三项开放局限**而非编造答复：
  (i) 相对记录的执行 ground-truth 的按链码模板漏检率；
  (ii) 端到端延迟分布与 RecycleBucket 饥饿上界；
  (iii) 多组织下的网关信任模型。
  三项均可在终稿中作为待补内容标注，且不会动摇论文的中心论断。
